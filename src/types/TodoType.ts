//Defining the type of todo elements
export interface TodoType {
    id: number,
    title: string,
    description: string, 
    priority: string,
    isChecked: boolean,
    isEditing: boolean
  }

